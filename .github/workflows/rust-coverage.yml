on:
  workflow_call:
    inputs:
      enabled:
        description: Whether to run coverage
        required: true
        type: boolean
      container-image:
        description: Docker image to run the job in
        required: false
        default: ''
        type: string

permissions:
  contents: read

jobs:
  Coverage:
    # allows callers to skip coverage report
    if: ${{ inputs.enabled }}
    runs-on: ubuntu-latest
    container: ${{ inputs.container-image || '' }}
    steps:
      - uses: tylerkelly13/.github/.github/actions/cargo-setup@main
        with:
          container-image: ${{ inputs.container-image }}
      # On bare runners, install cargo-tarpaulin for coverage reporting
      - name: Install cargo-tarpaulin
        if: inputs.container-image == ''
        uses: taiki-e/install-action@0fd46367812ee04360509b4169d9f659d6892bb2  # v2
        with:
          tool: cargo-tarpaulin
      - name: Report test coverage
        run: just coverage
