on:
  workflow_call:
    inputs:
      node-version:
        required: true
        type: string

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: tylerkelly13/.github/.github/actions/node-setup@main
        with:
          node-version: ${{ inputs.node-version }}
      - name: Run tests
        run: pnpm test:coverage
